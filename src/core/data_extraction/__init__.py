# Processor modules package initialization
# Defines what is imported when the package "processors" is imported
from .scene_extractor import SceneExtractor
from .video_processor import VideoProcessor


